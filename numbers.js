function sum(x, y) {
  return x + y;
}

function sub(x, y) {
  return x - y;
}

function mult(x, y) {
  return x * y;
}

function div(x, y) {
  return x / y;
}

// Exportação
module.exports.sum = sum;
module.exports.sub = sub;
module.exports.mult = mult;
module.exports.div = div;